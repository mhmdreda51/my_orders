import 'package:flutter/material.dart';

class FinishingPayment extends StatelessWidget {
  const FinishingPayment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
